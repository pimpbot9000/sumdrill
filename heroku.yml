setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

build:
  docker:
    web: Dockerfile.production2
    backend: backend/Dockerfile
  config:
    RAILS_ENV: development
    FOO: bar

run:
  web: npm start
  backend: npm start